name: plugin-surface-audit
on:
  push:
  pull_request:
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Audit declared plugin paths
        run: python3 tools/plugin_surface_audit.py
